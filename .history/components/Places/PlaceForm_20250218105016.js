import { ScrollView, Text, TextInput, View } from "react-native";

function PlaceForm() {
    return (
        <ScrollView>
            <View>
                <Text>Title</Text>
                <TextInput
        </ScrollView>
    
    
    );
}

export default PlaceForm;