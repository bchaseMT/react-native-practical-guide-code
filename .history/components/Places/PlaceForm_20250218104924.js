import { ScrollView, Text, View } from "react-native";

function PlaceForm() {
    return (
        <ScrollView>
            <View>
                <Text>Place Form</Text>
        </ScrollView>
    
    
    );
}

export default PlaceForm;